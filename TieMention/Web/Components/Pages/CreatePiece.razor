@rendermode InteractiveServer
@inject HttpClient Http


@page "/Piece/Create"

<div class="container">
    <SearchBar/>

    <div>    
        <MainTitle Title="Create Piece"/>
    </div>

    <FormCreatePiece 
        OnSubmit="HandleFormSubmit"
        @ref="FormCreatePieceComponent"/>
</div>



@code {
    private FormCreatePiece? FormCreatePieceComponent;
    
    private async Task HandleFormSubmit(FormPieceModel content)
    {
        try
        {
            var response = await Http.PostAsJsonAsync("api/piece", content);
            
            if (response.IsSuccessStatusCode)
            {
                // Handle success
                if (FormCreatePieceComponent != null)
                {
                    await FormCreatePieceComponent.ShowSuccessModal();
                }
            }
            else
            {
                // Handle error
                Console.WriteLine($"Error: {response.StatusCode}");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Exception: {ex.Message}");
        }
    }
}