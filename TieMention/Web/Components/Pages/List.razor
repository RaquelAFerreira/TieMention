@rendermode InteractiveServer
@page "/list/{searchTerm}"
@page "/list"
@using TieMention.Services
@inject PieceService PieceService

<div class="container">
    <ListPage SearchTerm="@searchTerm"/>
</div>

@code {
    [Parameter]
    public string searchTerm { get; set; } = string.Empty;
}